<Window x:Class="HouseholdAccountBook.Views.Windows.ProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HouseholdAccountBook.Views.Windows"
        xmlns:properties="clr-namespace:HouseholdAccountBook.Properties"
        xmlns:vmw="clr-namespace:HouseholdAccountBook.ViewModels.Windows"
        mc:Ignorable="d"
        Title="ProgressWindow" Height="100" Width="200"
        WindowStyle="None" ResizeMode="NoResize" BorderThickness="1" BorderBrush="SkyBlue"
        Closing="Window_Closing">

    <Window.DataContext>
        <vmw:ProgressWindowViewModel x:Name="WVM"/>
    </Window.DataContext>

    <Window.Style>
        <StaticResource ResourceKey="BusyCursor"/>
    </Window.Style>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{x:Static properties:Resources.TextBlock_Processing}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <ProgressBar Grid.Row="1" Margin="5" Value="{Binding ProgressValue}" IsIndeterminate="{Binding IsIndeterminate}"/>
        <Button Grid.Row="2" Margin="5" Content="{x:Static properties:Resources.Button_Cancel}" Command="{Binding CancelCommand}"/>
    </Grid>
</Window>
